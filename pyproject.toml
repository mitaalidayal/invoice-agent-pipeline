[project]
name = "galatiq-case-invoices"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Mitaali Dayal", email = "mitaalidayal26@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "langgraph>=1.2.11",
    "openai>=3.3.1",
    "pdfplumber>=0.11.10",
    "pydantic>=2.13.4",
    "python-dotenv>=1.2.3",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
markers = [
    "integration: makes real calls to the Grok API (slower, small real cost)",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
